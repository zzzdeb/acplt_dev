
/******************************************************************************
 *
 *   FILE
 *   ----
 *   reqReceiver.c
 *
 *   History
 *   -------
 *   2018-12-28   File created
 *
 *******************************************************************************
 *
 *   This file is generated by the 'acplt_builder' command
 *
 ******************************************************************************/

#ifndef OV_COMPILE_LIBRARY_lbalance
#define OV_COMPILE_LIBRARY_lbalance
#endif

#include "acplt_simpleMsgHandling.h"
#include "lbalance.h"
#include "lbalance_helper.h"
#include "libov/ov_macros.h"

#include "libov/ov_result.h"

OV_DLLFNCEXPORT OV_RESULT lbalance_reqReceiver_reset_set(
    OV_INSTPTR_lbalance_reqReceiver pobj, const OV_BOOL value) {
  OV_RESULT result = OV_ERR_OK;
  if(value && !pobj->v_reset) {
    pobj->v_status = 0;
    pobj->v_result = 0;
    result |= Ov_SetDynamicVectorLength(&pobj->v_reqIPs, 0, STRING);
    result |= Ov_SetDynamicVectorLength(&pobj->v_reqLoads, 0, UINT);
    result |=
        Ov_SetDynamicVectorLength(&pobj->v_reqLoadRequirements, 0, STRING);
  }
  pobj->v_reset = value;
  return result;
}

OV_DLLFNCEXPORT void
lbalance_reqReceiver_typemethod(OV_INSTPTR_fb_functionblock pfb,
                                OV_TIME*                    pltc) {
  /*
   *   local variables
   */
  OV_INSTPTR_lbalance_reqReceiver pinst =
      Ov_StaticPtrCast(lbalance_reqReceiver, pfb);

  OV_INSTPTR_PostSys_Message pMsg = NULL;
  OV_STRING                  MsgBody = NULL;
  OV_STRING                  command = NULL;
  OV_STRING                  value = NULL;
  OV_STRING                  order = NULL;
  OV_RESULT                  result = OV_ERR_OK;
  OV_STRING                  service = NULL;
  OV_STRING_VEC              ids = {0, NULL};
  OV_STRING_VEC              values = {0, NULL};
  OV_STRING_VEC              types = {0, NULL};
  OV_STRING_VEC              units = {0, NULL};

  OV_UINT waitingMsgsLen = 0;
  OV_UINT nLen = 0;
  OV_UINT currentInd = pinst->v_reqIPs.veclen;

  cJSON* jsMsg = NULL;
  cJSON* jsip = NULL;
  cJSON* jsload = NULL;
  cJSON* jsreqs = NULL;
  switch(pinst->v_status) {
    case LB_REQREC_INIT:
      /* pinst->p_inbox */

      Ov_ForEachChildEx(ov_containment, &pinst->p_inbox, pMsg,
                        PostSys_Message) {
        waitingMsgsLen++;
      }
      if(!waitingMsgsLen) {
        return;
      }
      nLen = pinst->v_reqIPs.veclen + waitingMsgsLen;
      result |= Ov_SetDynamicVectorLength(&pinst->v_reqIPs, nLen, STRING);
      result |= Ov_SetDynamicVectorLength(&pinst->v_reqLoads, nLen, UINT);
      result |= Ov_SetDynamicVectorLength(&pinst->v_reqLoadRequirements, nLen,
                                          STRING);
      if(Ov_Fail(result)) {
        ov_logfile_error(
            "lbalance_reqReceiver: %u: %s: failed to allocate vector", result,
            ov_result_getresulttext(result));
        pinst->v_status = LB_INTERNALERROR;
        pinst->v_result = result;
        return;
      }
      ov_memstack_lock();
      Ov_ForEachChildEx(ov_containment, &pinst->p_inbox, pMsg,
                        PostSys_Message) {
        /* parse and edit output */
        MsgBody = PostSys_Message_msgBody_get(pMsg);
        /*	DEBUG: print MSG-Body	*/
        /*	ov_logfile_debug("Msg-Body:\n\n%s\n\n", MsgBody);	*/
        result =
            acplt_simpleMsg_parseFlatBody(MsgBody, &service, &command, FALSE,
                                          NULL, &ids, &values, &units, &types);
        if(Ov_Fail(result)) {
          ov_logfile_warning(
              "lbalance_reqReceiver: parsing msg failed %s %d %s",
              pinst->v_identifier, result, ov_result_getresulttext(result));
          Ov_DeleteObject(pMsg); /*	Message is corrupted --> delete it
                                  */
          continue;
        }
        /*	check message	*/
        if(ids.veclen > 2 || ids.veclen < 1) {
          ov_logfile_info("%s: malformed message --> deleting it",
                          pinst->v_identifier);
          Ov_DeleteObject(pMsg); /*	Message is corrupted --> delete it
                                  */
          continue;
        }
        if(ids.veclen == 2) {
          if(ids.value[1] &&
             ov_string_compare(ids.value[1], "Value") != OV_STRCMP_EQUAL) {
            ov_logfile_info("%s: malformed message --> deleting it",
                            pinst->v_identifier);
            Ov_DeleteObject(pMsg); /*	Message is corrupted --> delete it
                                    */
            continue;
          }
        }
        /*	get service	and Operation	*/
        if(ov_string_compare(service, "ProcessControl") != OV_STRCMP_EQUAL) {
          ov_logfile_info("%s: unknown service requested --> deleting message",
                          pinst->v_identifier);
          Ov_DeleteObject(pMsg); /*	Message is corrupted --> delete it
                                  */
          continue;
        }

        if(ids.veclen == 2 && values.value[1]) { /*	value found	*/
          value = values.value[1];
        } else { /*	now value found, set zo 0	*/
          value = "0";
        }
        order = ov_memstack_alloc(
            strlen(values.value[0]) + strlen(command) + strlen(value) +
            3); /*	commander;command;value\0	*/
        if(!order) {
          ov_memstack_unlock();
          ov_logfile_info("%s: could not create order, heap is out of memory",
                          pinst->v_identifier);
          return;
        }

        jsMsg = cJSON_Parse(value);
        ov_logfile_debug("lbalance_reqReceiver: push: %s", value);
        if(!jsMsg || !cJSON_IsArray(jsMsg) ||
           cJSON_GetArraySize(jsMsg) != LB_REQREC_JSONLEN) {
          ov_logfile_warning("lbalance_neighborDB: msg: bad json format");
          Ov_DeleteObject(pMsg);
          continue;
        }
        jsip = cJSON_GetArrayItem(jsMsg, LB_REQREC_IPPOS);
        if(!jsip || !cJSON_IsString(jsip)) {
          ov_logfile_warning("lbalance_neighborDB: msg: bad json format at %d",
                             LB_REQREC_IPPOS);
          Ov_DeleteObject(pMsg);
          continue;
        }

        jsload = cJSON_GetArrayItem(jsMsg, LB_REQREC_LOADPOS);
        if(!jsload || !cJSON_IsNumber(jsload)) {
          ov_logfile_warning("lbalance_neighborDB: msg: bad json format at %d",
                             LB_REQREC_LOADPOS);
          Ov_DeleteObject(pMsg);
          continue;
        }

        jsreqs = cJSON_GetArrayItem(jsMsg, LB_REQREC_REQUIREPOS);
        if(!jsreqs || !cJSON_IsString(jsreqs)) {
          ov_logfile_warning("lbalance_neighborDB: msg: bad json format at %d",
                             LB_REQREC_REQUIREPOS);
          Ov_DeleteObject(pMsg);
          continue;
        }

        ov_string_setvalue(&pinst->v_reqIPs.value[currentInd],
                           jsip->valuestring);
        pinst->v_reqLoads.value[currentInd] = jsload->valueint;
        ov_string_setvalue(&pinst->v_reqLoadRequirements.value[currentInd],
                           jsreqs->valuestring);

        currentInd++;

        cJSON_Delete(jsMsg);
        Ov_DeleteObject(pMsg);
      }
      ov_memstack_unlock();

      result |= Ov_SetDynamicVectorLength(&pinst->v_reqIPs, currentInd, STRING);
      result |= Ov_SetDynamicVectorLength(&pinst->v_reqLoads, currentInd, UINT);
      result |= Ov_SetDynamicVectorLength(&pinst->v_reqLoadRequirements,
                                          currentInd, STRING);
      if(Ov_Fail(result)) {
        ov_logfile_error(
            "lbalance_neighborDB: %u: %s: failed to allocate vector "
            "with currentInd",
            result, ov_result_getresulttext(result));
        pinst->v_status = LB_INTERNALERROR;
        pinst->v_result = result;
        return;
      }

      break;

      LB_CASEERROR;
  }

  return;
}

